# Labs
## Instructions
### Lab 1: Threads
* "Add project from disk" in Unity and select the Lab1_Threads folder
* To change from Bubble Sort to Insertion Sort:
  * Inside the scripts folder, open the BubbleSort.cs script
  * Comment line 32 (sortThread = new Thread(bubbleSort);) and uncomment line 35 (sortThread = new Thread(insertionSort);)
